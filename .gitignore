# Thư mục node_modules (chứa các dependencies cài bằng npm/yarn/pnpm)
node_modules/

# Các file lock nếu không muốn commit (chỉ giữ 1 file lock nếu cần)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Các build artifacts (nếu có)
dist/
build/

# Thư mục cache của Vite
.vite/

# Các tệp môi trường (nếu có)
.env
.env.local
.env.development
.env.production
.env.test

# Các file log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Thư mục của IDE (VSCode, JetBrains, ...)
.vscode/

# Các file hệ thống
.DS_Store
Thumbs.db
